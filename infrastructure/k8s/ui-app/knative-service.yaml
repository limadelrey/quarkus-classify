apiVersion: serving.knative.dev/v1 # Current version of Knative
kind: Service
metadata:
  name: quarkus-hackaton-ui-app # The name of the app
  namespace: default # The namespace the app will use
spec:
  template:
    spec:
      containers:
        - image: docker.io/limadelrey/quarkus-hackaton-ui-app:1.0 # Reference to the image of the app